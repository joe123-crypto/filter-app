
// A central configuration file for the application.
// This helps avoid duplicating keys and settings across multiple files.

export const firebaseConfig = {
  // NOTE: The API_KEY is now sourced from the `process.env.API_KEY`
  // environment variable in the services that require it.
  authDomain: "database-8982a.firebaseapp.com",
  projectId: "database-8982a",
  storageBucket: "database-8982a.appspot.com",
};
